stages:
- name: compile
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: dbd.sh
  when:
    branch:
      include:
      - master
- name: deploy
  steps:
  - runScriptConfig:
      image: python:latest
      shellScript: echo "Hello World"
timeout: 60
notification: {}
